---
interface Props {
  id: string;
  title: string;
}

const { id, title } = Astro.props;
---

<section id={id} class="scroll-mt-24 section-card">
  <h2 class="text-2xl">{title}</h2>
  <div class="mt-5 space-y-4">
    <slot />
  </div>
</section>
